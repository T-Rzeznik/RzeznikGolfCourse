# Copy this file to `.env` and fill in your key. `.env` is gitignored.
# Get a key (free tier available) at https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Optional overrides:
# GEMINI_MODEL=gemini-2.5-flash-lite   # cheap default; gemini-2.5-flash hedges a bit better
# CHATBOT_PORT=8000
